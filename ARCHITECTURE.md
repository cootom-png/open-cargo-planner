# Open Cargo Planner 系统架构

## 1. 架构目标

Open Cargo Planner 需要把三类能力分离：业务数据管理、装载优化求解、三维方案呈现。核心目标是让求解器可替换、约束可扩展、结果可复现，并防止长时间计算阻塞普通业务请求。

## 2. 系统边界

```text
┌──────────────────────────────────────────────────────┐
│ Web 应用                                             │
│ 产品录入 · 柜型选择 · 约束设置 · 方案比较 · 3D 展示 │
└──────────────────────────┬───────────────────────────┘
                           │ HTTPS / JSON
┌──────────────────────────▼───────────────────────────┐
│ 业务 API                                             │
│ 认证 · 主数据 · 方案版本 · 输入校验 · 单位归一化    │
└──────────────┬─────────────────────────┬─────────────┘
               │                         │
       PostgreSQL                    任务队列
                                         │
┌────────────────────────────────────────▼─────────────┐
│ 求解编排服务                                         │
│ 预处理 · 托盘化 · 多求解器运行 · 校验 · 评分 · 排名 │
└──────────────┬─────────────────────────┬─────────────┘
               │                         │
        Java solver adapter        C++ solver adapter
        xflp / skjolber            PackingSolver
```

## 3. 两条求解链路

### 3.1 散装

```text
产品实例化 → 尺寸/重量下界检查 → 生成允许朝向 → 三维装箱
→ 可行性复核 → 重心与利用率评分 → 输出装柜顺序
```

### 3.2 托盘

```text
产品 + 多规格托盘 → 为各托盘生成候选码放 → 选择托盘组合
→ 生成不可拆分托盘载荷对象 → 多规格托盘装柜求解
→ 两阶段数量/重量/几何复核 → 输出各规格用量、码放图和装柜图
```

混装模式将已完成托盘与尾数散箱统一转换成 `LoadUnit`，再进入第二阶段。混托在码托阶段记录逐托 SKU 明细；尾数散箱可填充剩余空间，但不得侵入卡板 50 mm 安全区。

## 4. 核心领域模型

### ProductType

- SKU、名称、长宽高、单箱重量、需求数量。
- 水平旋转、侧装、倒置分别控制；禁止侧装时保持原始高度轴垂直。
- 是否可叠放、顶部最大承重。
- 易碎、必须正放、优先级、装卸站点等约束。

### PalletType

- 托盘编号、规格名称、长宽高、自重、最大载重和采购单价；卡板按无限供应处理，结果输出建议采购数。
- 最大含托高度、允许外伸量、柜内水平旋转和叉孔方向；完整卡板禁止上下叠放。
- 卡板前后左右、卡板与柜壁及柜门端均至少保留 50 mm 水平净距，由间距校验器使用最终外廓检查。
- 一个方案可同时选择多种托盘；产品可指定托盘、限定可用集合或交由系统自动选择。

### ContainerType

- 内部长宽高、门宽门高、最大载重、可用数量。
- 障碍物、禁放区和柜型成本。

### LoadUnit

求解器使用的统一货物对象，可以是单箱、箱组或完整托盘，包含尺寸、重量、朝向和承重约束。

### Placement

记录货物所在容器、`x/y/z` 坐标、最终尺寸、朝向、支撑关系和装载序号。

### Plan

保存输入快照、约束快照、求解器版本、随机种子、计算时限、Placements、未装列表、指标和警告。保存快照是结果可复现的前提。

## 5. 求解器适配层

统一接口示意：

```ts
interface SolverAdapter {
  solve(input: SolverInput, options: SolveOptions): Promise<SolverResult>;
  validate(result: SolverResult, input: SolverInput): ValidationReport;
}
```

适配器负责：

1. 将领域模型转换为具体求解器格式。
2. 设置计算时限和随机种子。
3. 标准化坐标和朝向。
4. 将结果转换成统一结构。
5. 独立复核碰撞、越界、重量和数量守恒。

第一阶段建议接入：

- `xflp`：承重、堆叠、装卸顺序等现实约束。
- `3d-bin-container-packing`：毫秒级快速候选方案。

正式环境可以并行运行多个适配器，用统一评分函数选择最佳可行结果。

## 6. 方案评分

建议使用分层目标，而不是将所有指标简单相加：

1. 硬约束必须全部满足。
2. 根据 SKU 分配策略生成候选；默认按单件包装体积从大到小放置，再用小件填充。
3. 最大化订单完成率或装载价值。
4. 最小化使用柜数和运输成本。
5. 最大化体积利用率。
6. 改善重心、支撑率和装卸可执行性。

每项评分必须可解释，并在结果页显示未装原因，如尺寸不匹配、载重达到上限、承重冲突或计算时限内未找到位置。

## 7. 异步任务

求解请求通过任务队列执行：

```text
PENDING → PREPROCESSING → SOLVING → VALIDATING → COMPLETED
                                      └────────→ FAILED
```

前端通过轮询或 SSE 获取状态。每个任务设置 CPU、内存和执行时限；取消任务时终止对应求解进程。

## 8. 三维呈现

前端采用 Three.js：

- 使用同一毫米坐标系，渲染时统一缩放。
- 不同 SKU 使用稳定颜色映射。
- 支持旋转、缩放、柜体透明度、分层过滤和装载顺序播放。
- 点击货物显示 SKU、坐标、朝向、重量和支撑关系。
- 3D 只是结果视图，不能替代服务端独立可行性校验。

## 9. 数据与接口

核心 API 建议：

```text
POST /api/plans                 创建方案
POST /api/plans/{id}/solve      发起求解
GET  /api/jobs/{id}             查询进度
GET  /api/plans/{id}/results    获取候选结果
POST /api/results/{id}/verify   重新校验
GET  /api/results/{id}/export   导出装柜指引
```

数据库至少包括产品、托盘、柜型、方案、方案输入快照、求解任务、求解结果和放置明细。

## 10. 安全与正确性

- 所有尺寸统一转换成整数毫米，重量统一成克或整数精度单位。
- 文件导入限制大小和行数，不执行上传文件中的公式或宏。
- 外部求解进程运行在受限容器内。
- 不信任求解器输出，必须执行独立几何和重量校验。
- 记录求解器版本、输入哈希、参数和随机种子。
- 许可证声明与第三方 NOTICE 随发布物一起保留。

## 11. 演进路线

### MVP

单柜型、长方体产品、散装/单品托、旋转控制、重量限制、3D 结果和 Excel/PDF 导出。

### 第二阶段

混托、混装、产品承重、重心、障碍物、装卸顺序和多柜型成本比较。

### 第三阶段

订单拆分、多目的地、装柜现场移动端指引、历史方案学习和企业系统集成。
