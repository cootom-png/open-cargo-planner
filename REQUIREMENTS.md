# Open Cargo Planner 产品需求

> 状态：Draft 0.1  
> 更新日期：2026-08-22  
> 适用范围：MVP 与后续演进需求基线

## 1. 文档目的

本文档定义 Open Cargo Planner 的业务范围、用户流程、功能需求、约束规则、数据模型和验收标准。产品用于规划长方体货物的托盘码放和集装箱装载，输出可解释、可复核、可供现场执行的方案。

三维装箱属于 NP-hard 问题。系统承诺的是“在指定约束和计算时限内找到的最佳可行方案”，不承诺在所有问题规模下证明全局最优。

## 2. 调研结论

同类成熟工具和行业资料体现出一套稳定需求组合：

- 支持散箱、完整托盘和二者混装。
- 支持多 SKU、多柜型、多容器以及自动拆分。
- 产品、包装、托盘、柜体应作为可复用主数据。
- 允许逐 SKU 设置旋转、正放、叠放、承重和优先级。
- 不能只检查总体积和总重量，还必须检查柜门、碰撞、支撑、重心与集中载荷。
- 多规格托盘是现实业务的基础能力，系统需要比较不同托盘及其组合，而不是只接受一个托盘尺寸。
- 结果需要三维查看、分层图、俯视/侧视图、装载顺序、明细表和未装原因。
- Excel/CSV 导入、PDF/Excel 导出、方案保存和分享是常见工作流。
- 高级场景还包括多目的地卸货顺序、货物隔离、危险品规则、绑带和填充材料空间。

## 3. 产品目标

### 3.1 业务目标

1. 快速回答货物是否能装下，以及各种 SKU 分别能装多少。
2. 对比散装、托盘化及不同柜型的空间和成本表现。
3. 减少因柜门、超重、压坏、偏载或错误顺序造成的现场返工。
4. 将个人装柜经验沉淀成可复用规则和可审计方案。

### 3.2 质量目标

1. 碰撞、越界、数量不守恒和超载结果必须为零。
2. 所有正式结果必须经过独立校验器复核。
3. 相同输入快照、求解器版本、参数和随机种子应可复现。
4. 每个未装货物必须给出明确原因或“计算时限内未找到可行位置”。

## 4. 用户角色

### 4.1 计划员

导入出货清单，配置约束，运行求解，比较并确认方案。

### 4.2 仓库与装柜人员

通过手机或打印文件查看装载顺序、分层图和每件货物位置，反馈现场偏差。

### 4.3 业务或报价人员

快速判断一个订单需要的柜型、柜数、托盘数及大致利用率。

### 4.4 管理员

维护产品、托盘、柜型、默认约束、用户权限和企业级模板。

## 5. 核心业务场景

### S1：产品直接散装

输入多种产品及数量，选择一个或多个柜型，系统直接把产品箱装入集装箱。

### S2：单一规格托盘装载

产品先码放到指定托盘，再将完整托盘装柜。输出每托箱数、层数、总高、总重和托盘装柜位置。

### S3：多规格托盘自动选择

存在多种尺寸和数量的卡板。系统为每个产品或产品组合生成候选码放方案，比较后选择托盘规格及使用数量。

### S4：多规格托盘混用

一个柜内可以出现多种尺寸的完整托盘。系统需要考虑托盘方向、叉车间隙和装卸可行性。

### S5：托盘与散箱混装

部分货物必须打托，剩余货物允许散装填充空隙。系统分别统计整托与散箱数量。

### S6：多柜型与多柜拆分

系统在指定可用数量和成本条件下选择 20GP、40GP、40HQ 或自定义柜体，并将货物分配到多个柜。

### S7：现场执行

仓库人员按步骤查看“下一件/下一托”，通过 SKU、颜色、坐标、朝向和视图完成装载。

## 6. 端到端流程

```text
创建方案
→ 导入或选择产品
→ 选择散装/托盘/混装
→ 选择托盘和柜型
→ 配置必要约束与优化目标
→ 数据预检查
→ 异步求解
→ 独立校验
→ 比较候选方案
→ 确认并导出
→ 记录现场反馈
```

## 7. 功能需求

优先级定义：P0 为 MVP 必须，P1 为首个可用版本，P2 为后续增强。

### 7.1 产品与包装资料

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-P-001 | P0 | 支持手工新增、编辑和删除产品。 |
| FR-P-002 | P0 | 每个产品包含 SKU、名称、外包装长宽高、数量和单箱毛重。 |
| FR-P-003 | P0 | 尺寸必须使用实际运输外廓尺寸，而不是裸产品尺寸。 |
| FR-P-004 | P0 | 支持六种正交朝向的允许/禁止设置。 |
| FR-P-005 | P0 | 支持“必须正放”和“不可叠放”。 |
| FR-P-006 | P1 | 支持最大顶部承重、最大堆叠层数、易碎属性。 |
| FR-P-007 | P1 | 支持产品优先级和允许部分装载。 |
| FR-P-008 | P1 | 支持 Excel/CSV 批量导入，并逐行反馈错误。 |
| FR-P-009 | P2 | 支持包装层级：单件、内箱、外箱、托盘。 |
| FR-P-010 | P2 | 支持圆柱和使用包围盒近似的异形件。 |

### 7.2 多规格托盘资料

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-PL-001 | P0 | 一个方案可选择零种、一种或多种托盘规格。 |
| FR-PL-002 | P0 | 每种托盘包含编号、名称、长、宽、高、数量和自重。 |
| FR-PL-003 | P0 | 每种托盘包含最大载重和最大含托高度。 |
| FR-PL-004 | P0 | 支持设置是否允许托盘在柜内水平旋转 90°。 |
| FR-PL-005 | P0 | 支持产品指定托盘、允许托盘集合或自动选择托盘。 |
| FR-PL-006 | P0 | 求解结果必须统计各托盘规格使用数、剩余数及每托内容。 |
| FR-PL-007 | P1 | 支持允许外伸量，默认值为 0。 |
| FR-PL-008 | P1 | 支持单品托和混合 SKU 托盘。 |
| FR-PL-009 | P1 | 支持限制每托 SKU 数量、整层码放和层间交错。 |
| FR-PL-010 | P1 | 支持托盘顶面承重、是否允许双层叠托。 |
| FR-PL-011 | P2 | 支持托盘材质、叉孔方向和接收方指定托盘标准。 |
| FR-PL-012 | P2 | 支持保留指定数量托盘，不全部投入本次方案。 |

### 7.3 柜型与装载空间

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-C-001 | P0 | 内置 20GP、40GP、40HQ，并允许维护自定义柜型。 |
| FR-C-002 | P0 | 柜型包含内部长宽高、门宽门高、最大有效载荷和可用数量。 |
| FR-C-003 | P0 | 货物必须能以允许朝向通过柜门；内部能容纳但无法进门时判定不可行。 |
| FR-C-004 | P0 | 支持同一方案使用多个集装箱。 |
| FR-C-005 | P1 | 支持柜内障碍物和禁放区。 |
| FR-C-006 | P1 | 支持墙面、顶部、货物间和门端预留间隙。 |
| FR-C-007 | P1 | 支持冷藏柜、开顶柜等扩展柜型模板。 |
| FR-C-008 | P2 | 支持地板单位面积或单位长度集中载荷限制。 |

### 7.4 装载模式

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-M-001 | P0 | 支持散装模式。 |
| FR-M-002 | P0 | 支持托盘模式，执行“产品码托 → 完整托盘装柜”的两阶段求解。 |
| FR-M-003 | P1 | 支持同柜内多规格托盘混用。 |
| FR-M-004 | P1 | 支持托盘与散箱混装。 |
| FR-M-005 | P1 | 支持固定某 SKU 必须打托、禁止打托或自动决定。 |
| FR-M-006 | P2 | 支持对已有完整托盘直接录入，不重新计算托内结构。 |

### 7.5 约束与可行性

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-R-001 | P0 | 所有货物必须在容器边界内，任意两件货物不得重叠。 |
| FR-R-002 | P0 | 装载数量不得超过需求数量，托盘和柜体用量不得超过可用数量。 |
| FR-R-003 | P0 | 容器总载重不得超限，托盘总重不得超过托盘限载。 |
| FR-R-004 | P0 | 非地面货物必须达到最低底面支撑比例；默认建议 100%，允许按规则调整。 |
| FR-R-005 | P1 | 每件产品承受的上方累计重量不得超过顶部承重。 |
| FR-R-006 | P1 | 重货优先低位，易碎品不得承托不允许的货物。 |
| FR-R-007 | P1 | 检查整体重心和左右、前后重量分布并给出风险等级。 |
| FR-R-008 | P1 | 支持货物分组、必须相邻、禁止混装和客户隔离。 |
| FR-R-009 | P1 | 支持多目的地后进先出和门端可达性。 |
| FR-R-010 | P2 | 支持绑带、气囊、木方等加固材料所需空间。 |
| FR-R-011 | P2 | 支持危险品、温区和兼容性规则；系统仅执行配置，不替代合规审核。 |

### 7.6 优化目标

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-O-001 | P0 | 支持优先完成订单或最大化已装数量。 |
| FR-O-002 | P0 | 支持最小化使用柜数。 |
| FR-O-003 | P0 | 支持最大化体积利用率。 |
| FR-O-004 | P1 | 支持最小化柜型与托盘综合成本。 |
| FR-O-005 | P1 | 支持改善重心、支撑和现场可执行性。 |
| FR-O-006 | P1 | 支持生成至少三个候选方案并解释主要取舍。 |
| FR-O-007 | P2 | 支持用户配置业务优先级，但硬约束不得通过权重降低而违反。 |

推荐采用分层目标：先满足硬约束，再最大化订单完成率，然后比较柜数、成本、利用率、平衡性和操作难度。

### 7.7 求解任务

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-S-001 | P0 | 求解以异步任务执行，页面显示排队、预处理、求解、校验和完成状态。 |
| FR-S-002 | P0 | 用户可选择快速、平衡和深度计算时限。 |
| FR-S-003 | P0 | 用户可以取消尚未完成的任务。 |
| FR-S-004 | P0 | 任务必须保存输入快照、参数、求解器版本和随机种子。 |
| FR-S-005 | P0 | 求解器输出必须由独立校验器检查后才能标记为可用。 |
| FR-S-006 | P1 | 可并行运行多个求解器或多组种子，并按统一标准评分。 |
| FR-S-007 | P1 | 修改输入后保留旧版本结果，不静默覆盖。 |

### 7.8 结果与可视化

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-V-001 | P0 | 显示总需求、已装、未装、柜数、托盘数、体积和重量利用率。 |
| FR-V-002 | P0 | 按 SKU 显示已装和剩余数量。 |
| FR-V-003 | P0 | 按托盘规格显示使用数量，并可展开查看每托内容。 |
| FR-V-004 | P0 | 提供可旋转、缩放和平移的三维视图。 |
| FR-V-005 | P0 | 提供俯视、侧视、门端视图和按层查看。 |
| FR-V-006 | P0 | 不同 SKU 使用稳定且可辨识的颜色，并支持搜索定位。 |
| FR-V-007 | P0 | 点击货物显示 SKU、坐标、尺寸、朝向、重量和装载序号。 |
| FR-V-008 | P0 | 明确列出每个未装项目的原因。 |
| FR-V-009 | P1 | 显示重心位置、风险等级及左右/前后重量分布。 |
| FR-V-010 | P1 | 支持逐步播放装载顺序和隐藏已装步骤。 |
| FR-V-011 | P1 | 支持用户锁定、移动、旋转或删除货物后重新求解其余空间。 |
| FR-V-012 | P2 | 支持生成只读分享链接供仓库和客户查看。 |

### 7.9 导入、导出与保存

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-I-001 | P0 | 支持保存方案草稿和求解结果。 |
| FR-I-002 | P1 | 支持 Excel/CSV 产品和完整托盘导入模板。 |
| FR-I-003 | P1 | 导入前展示字段映射、单位和错误预览。 |
| FR-I-004 | P1 | 支持导出 Excel 明细、PDF 装载指引和方案 JSON。 |
| FR-I-005 | P1 | PDF 至少包含方案摘要、SKU 汇总、托盘汇总、视图和装载顺序。 |
| FR-I-006 | P2 | 支持复制方案、版本比较和审计记录。 |

## 8. 关键业务规则

### BR-001 单位归一化

尺寸在服务端统一为整数毫米，重量统一为克或可控整数精度。原始单位和原始值保留用于展示与审计。

### BR-002 数量守恒

每个 SKU 必须满足：

```text
需求数量 = 已装数量 + 未装数量
```

每种托盘和柜型的使用数量不得超过本次可用数量。

### BR-003 托盘有效尺寸

托盘载荷的外廓尺寸应包含产品外伸、缠绕膜、护角等包装增量。未设置允许外伸时，任何货物不得超出托盘边界。

### BR-004 托盘两阶段守恒

产品进入托盘后形成不可拆分 `PalletLoad`。装柜阶段不得改变托内箱体位置；若需改变，必须重新运行码托求解。

### BR-005 柜门可进入性

每件散箱和完整托盘至少存在一种允许姿态可通过柜门，并有足够路径进入最终位置。MVP 至少执行门宽门高静态检查，P1 增加路径可达性检查。

### BR-006 支撑与承重

货物在地板或其他货物顶部的有效接触面积不得低于配置比例；其承担的累计上部重量不得超过限制。

### BR-007 未装原因

原因按确定性优先级返回：无可用柜体、无可用托盘、单件尺寸超限、柜门不通过、重量超限、方向限制、承重冲突、分组冲突、装卸顺序冲突、计算时限未找到位置。

### BR-008 方案状态

只有通过独立校验器的结果才能标记为 `VALID`。校验失败的候选结果可用于诊断，但不得导出为正式装载指引。

## 9. 核心数据模型

### ProductType

```ts
interface ProductType {
  id: string;
  sku: string;
  name: string;
  lengthMm: number;
  widthMm: number;
  heightMm: number;
  weightG: number;
  quantity: number;
  allowedOrientations: Orientation[];
  stackable: boolean;
  maxTopLoadG?: number;
  maxStackLayers?: number;
  palletPolicy: "required" | "forbidden" | "auto";
  eligiblePalletTypeIds?: string[];
  priority: number;
}
```

### PalletType

```ts
interface PalletType {
  id: string;
  code: string;
  name: string;
  lengthMm: number;
  widthMm: number;
  heightMm: number;
  quantity: number;
  emptyWeightG: number;
  maxLoadG: number;
  maxLoadedHeightMm: number;
  overhangMm: number;
  allowHorizontalRotation: boolean;
  allowDoubleStack: boolean;
}
```

### ContainerType

```ts
interface ContainerType {
  id: string;
  code: string;
  name: string;
  innerLengthMm: number;
  innerWidthMm: number;
  innerHeightMm: number;
  doorWidthMm: number;
  doorHeightMm: number;
  maxPayloadG: number;
  quantity: number;
  cost?: number;
}
```

### PalletLoad

```ts
interface PalletLoad {
  id: string;
  palletTypeId: string;
  outerLengthMm: number;
  outerWidthMm: number;
  outerHeightMm: number;
  totalWeightG: number;
  placements: ProductPlacement[];
  skuSummary: Record<string, number>;
}
```

### Placement

```ts
interface Placement {
  loadUnitId: string;
  containerId: string;
  xMm: number;
  yMm: number;
  zMm: number;
  lengthMm: number;
  widthMm: number;
  heightMm: number;
  orientation: Orientation;
  loadingSequence: number;
  supportedByIds: string[];
}
```

## 10. 输入校验

求解前必须阻止以下输入：

- 长、宽、高、数量或重量小于等于零。
- SKU 重复且字段冲突。
- 产品不存在任何允许朝向。
- 托盘最大含托高度小于托盘自身高度。
- 托盘限载小于托盘自重。
- 柜体门尺寸或内部尺寸缺失。
- 产品被设置为必须打托，但没有任何可用且尺寸合适的托盘。
- 优先级、数量或计算规模超过当前系统限制。

系统应警告但允许用户确认继续：

- 缺少顶部承重数据。
- 使用默认间隙或默认支撑率。
- 总 CBM 明显超过可用柜容。
- 数据精度或单位疑似异常。

## 11. MVP 范围

### 11.1 MVP 包含

- 浏览器端产品清单录入。
- Excel/CSV 导入基础字段。
- 多 SKU 长方体货物。
- 多种托盘规格、数量限制和产品适用托盘。
- 散装和单品托盘模式。
- 20GP、40GP、40HQ 与自定义柜型。
- 水平旋转、必须正放、不可叠放。
- 尺寸、柜门、总重量、碰撞和基本支撑校验。
- 异步求解及计算时限。
- 至少一个快速求解器适配器。
- 3D、俯视、侧视、分层视图。
- SKU 与托盘使用汇总、未装原因。
- JSON 与 Excel 结果导出。

### 11.2 MVP 不包含

- 任意不规则形状的精确碰撞。
- 多目的地路线联动。
- 危险品法规自动判定。
- 机器人码垛路径控制。
- 完整的企业权限、审批和计费系统。
- 对所有输入规模证明全局最优。

## 12. MVP 验收标准

### AC-001 多规格托盘

给定两种以上托盘及各自有限数量，系统能够在同一方案中选择并使用多种规格；结果中各规格使用数不超过库存，并能展开查看每托 SKU 和数量。

### AC-002 数量守恒

任何求解结果中，每个 SKU 的已装数与未装数之和等于需求数，且不存在重复货物实例。

### AC-003 几何有效

独立校验器确认所有放置对象位于容器范围内且互不重叠；误差仅允许来自明确记录的整数单位换算。

### AC-004 重量有效

每个托盘不超过自身最大载重，每个柜不超过最大有效载荷，结果汇总与明细重量一致。

### AC-005 柜门检查

若货物或完整托盘在所有允许朝向下均无法通过柜门，系统不允许将其标记为已装，并返回柜门尺寸限制原因。

### AC-006 结果可理解

用户无需查看算法日志，即可看到各 SKU 装载数量、各托盘规格使用数、柜数、利用率、未装原因及三维位置。

### AC-007 计算可控

任务达到用户设置时限后必须终止或返回当前最佳有效解，不能无限运行。

### AC-008 原型与正式结果区分

未接入正式求解器前，页面必须明确显示“演示/模拟结果”，不得将体积比例估算描述为真实最优装载方案。

## 13. 非功能需求

### 性能

- 普通输入操作即时响应。
- 1000 个箱体实例以内的快速模式目标为 10 秒内返回首个有效方案。
- 所有求解设置硬性时限，并支持取消。

### 可用性

- 计划员可在桌面浏览器完成配置和三维调整。
- 仓库人员可在手机浏览器查看只读方案和装载步骤。
- 关键状态不能只依赖颜色表达。

### 安全

- 上传文件限制大小、类型和行数，不执行宏和公式。
- 求解进程隔离运行并限制 CPU、内存和执行时间。
- 分享链接应可撤销并默认不暴露客户敏感字段。

### 可维护性

- 领域模型与具体求解器格式解耦。
- 求解器升级需通过固定基准数据集回归。
- 坐标、旋转和单位约定必须集中定义。

## 14. 需求待确认项

以下问题需要结合首批真实订单决定：

1. 卡板是否主要为单品托，还是混托同样常见？
2. 产品允许超出卡板边缘吗；四个方向是否使用同一限值？
3. 是否存在托盘双层叠放，完整托盘的顶部承重如何获取？
4. 是否需要把卡板、缠绕膜、护角和木方重量计入柜重？
5. 是否允许剩余散箱填充托盘之间或托盘顶部空间？
6. 业务更关心“尽量按订单比例装载”还是“优先装高价值/高优先级 SKU”？
7. 是否涉及多个目的港或卸货点？
8. 常用柜型的实际内部尺寸和最大载重是否由货代提供并需要按供应商保存？
9. 是否需要处理设备、圆桶、袋装等非长方体货物？
10. 现场需要 PDF、Excel、手机步骤页还是标签作为主要执行载体？

## 15. 建议验证数据集

正式开发前收集至少以下真实案例，并记录人工方案和实际装载结果：

- 单 SKU 散装整柜。
- 3–10 个 SKU 混合散装。
- 单一托盘规格、单品托。
- 两种以上托盘规格混用。
- 托盘不足，部分产品改用另一规格。
- 重货先达到载重上限。
- 单件内部能放下但无法通过柜门。
- 禁止侧放、不可叠放和顶部承重限制。
- 一个柜装不下，需要自动拆分到两个柜。
- 已知现场装载失败或返工的历史案例。

这些案例将作为求解质量和版本回归的基准，而不是只用随机生成数据测试。